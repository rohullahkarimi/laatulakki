declare type Step = {
    id: string;
};
declare type StepProps<S> = {
    steps: S[];
    initialStep?: number;
};
declare function useStep<S extends Step>({ steps, initialStep, }: StepProps<S>): {
    complete: (completeStep?: number | string) => void;
    completed: string[];
    index: number;
    navigation: {
        next: () => void;
        prev: () => void;
        go: (nextStep: number | string) => void;
    };
    step: S;
    uncomplete: (uncompleteStep?: number | string) => void;
    reset: (resetStep?: any) => void;
};
export default useStep;
//# sourceMappingURL=useStep.d.ts.map