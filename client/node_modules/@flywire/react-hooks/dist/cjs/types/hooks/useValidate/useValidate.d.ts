import { Data, Constraints, Options } from './types';
declare function useValidate(data?: Data, constraints?: Constraints, { onError, onSuccess }?: Options): {
    validate: () => boolean;
    errors: import("./types").Errors;
    isValid: boolean;
};
export default useValidate;
//# sourceMappingURL=useValidate.d.ts.map