import { CheckoutPayment, CheckoutRefund } from './types';
export declare class CheckoutClient {
    private algorithm;
    private secret;
    private merchantId;
    private baseUrl;
    constructor(merchantId: string, secret: string);
    calculateHmac(params: any, body: any): string;
    makeRequest(body: any, method: 'POST' | 'GET' | 'PUT', path: string, transactionId?: string, queryParams?: any): Promise<any>;
    createPayment(payment: CheckoutPayment): Promise<any>;
    /**
     * Get data of single payment
     * @param id transactionId from checkout
     */
    getPayment(id: string): Promise<any>;
    createRefund(refund: CheckoutRefund, id: string): Promise<any>;
    createEmailRefund(refund: CheckoutRefund, id: string): Promise<any>;
    getPaymentMethods(qs: any): Promise<any>;
    getPaymentMethodsGrouped(qs: any): Promise<any>;
}
export default CheckoutClient;
